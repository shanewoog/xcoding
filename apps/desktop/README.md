# Desktop Shell

This is a thin Tauri shell. In Phase 0 it verifies the Rust core through a Tauri command. The persistent local JSON-RPC sidecar connection is the next integration step after the stdio protocol is stabilized through the CLI.
